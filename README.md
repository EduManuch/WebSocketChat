# WebSocketChat

## 📌 Overview
**WebSocketChat** — это realtime‑чат, реализованный на Go с использованием WebSocket (RFC 6455). Проект включает:

- WebSocket‑сервер для обмена сообщениями
- Web‑клиент (HTML/JS)
- Docker‑окружение для удобного запуска

---

## 🚀 Features

- WebSocket‑чат (Gorilla WebSocket)
- Поддержка множества одновременных соединений
- Асинхронная обработка сообщений (producer / consumer)
- HTML‑клиент для ручного тестирования
- Docker / docker‑compose
- Поддержка TLS (self‑signed сертификаты)

---

## 🧱 Architecture

Проект следует стандартному Go Project Layout.

```
WebSocketChat/
├── internal/
│   └── wsserver/
│       ├── server.go      # WebSocket сервер
│       ├── producer.go    # Приём сообщений
│       ├── consumer.go    # Рассылка сообщений
│       └── dto.go         # DTO и модели
├── nginx/
│   └── nginx.conf
├── web/
│   ├── templates/         # HTML шаблоны
│   └── static/            # JS / CSS
├── Dockerfile
├── docker-compose.yml
├── Makefile
├── go.mod / go.sum
└── .env
```

### Message Flow

```
Client → Nginx → WebSocket → Producer → Channel → Consumer → Clients
```

Такой подход позволяет:
- минимизировать блокировки
- масштабировать обработку сообщений
- изолировать логику чтения и записи

---

## 🖥 Web Client

В директории `web/` расположен простой HTML/JS‑клиент для подключения к чату через браузер.

Подключение осуществляется через стандартный WebSocket API.

---


## 🔐 TLS (WSS)

Поддерживается работа через `wss://` с self‑signed сертификатами.
Отключить использование TLS на стороне приложения можно через переменную `BACKEND_TLS` (включено по умолчанию)
### Установка сертификатов (пример для Linux)
```bash
sudo apt install mkcert
mkcert -install
mkcert 127.0.0.1
```

---

# Makefile Commands

## 📖 Help

```bash
make
```
или
```bash
make help
```

Показывает список всех доступных целей с кратким описанием.

---

## 🐳 Docker / Docker Compose
Сервис будет доступен по портам, указанных в `.env`.

### `make up`
Собрать и запустить всё окружение.

- пересобирает образы
- пересоздаёт контейнеры
- запускает стек в фоне

После запуска сервисы будут доступны:
- WebSocket server 1: https://localhost:8443
- WebSocket server 2: https://localhost:8444
- Nginx balancer:     https://localhost
---

### `make start`
Запустить окружение **без пересборки образов**.

Используется для повторного старта уже созданных контейнеров.

---

### `make down`
Остановить и удалить все контейнеры.

- удаляет контейнеры
- удаляет volumes
- удаляет orphan-контейнеры

---

### `make clean`
Очистить Docker-окружение проекта.

- удаляет контейнеры
- удаляет локальные Docker-образы проекта
- удаляет orphan-контейнеры

---

### `make rebuild`
Полная пересборка проекта.

Эквивалентно выполнению:
```bash
make down
make up
```

---

### `make ps`
Показать список контейнеров и их текущий статус.

---

## ⚙️ Local Run
Запуск локально для отладки без Kafka.
```bash
make run
```
или
```bash
go run cmd/server/main.go
```

Перед запуском отключить Kafka через переменные окружения в `.env`.

---

## 🛠 Tech Stack

- Go
- Gorilla WebSocket
- HTML / JavaScript
- Docker
- Nginx

---

## 📚 References

- [RFC 6455 — WebSocket Protocol](https://datatracker.ietf.org/doc/html/rfc6455)
- [Gorilla WebSocket](https://github.com/gorilla/websocket)
- [Go Project Layout](https://github.com/golang-standards/project-layout)

---

## 🤝 Contribution

Проект является pet‑project, pull‑requests и идеи приветствуются.

---

